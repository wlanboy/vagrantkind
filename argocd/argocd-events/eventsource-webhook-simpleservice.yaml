apiVersion: argoproj.io/v1alpha1
kind: EventSource
metadata:
  name: webhook-simpleservice
  namespace: argo-workflows
spec:
  webhook:
    simpleservice:
      port: "12000"
      endpoint: /simpleservice
      method: POST
